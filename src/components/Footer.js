import React from 'react'
import styled from 'styled-components'

export default () => <Div>© Siwat Pruksapanya</Div>

const Div = styled.div`
  margin-top: 20px;
  border-top: 1px solid hsla(0, 0%, 0%, 0.07);
  color: hsla(0, 0%, 0%, 0.5);
`
